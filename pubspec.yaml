name: flutter_shaders_poc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.1
  vector_math: ^2.1.4
  camera: ^0.10.4
  flutter_blurhash:
    path: ../flutter_blurhash

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  shaders:
    - shaders/gradient.frag
    - shaders/warp.frag
    - shaders/sobel.frag
    - shaders/blurhash.frag
    - shaders/math.frag