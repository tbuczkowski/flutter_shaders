name: flutter_shaders_poc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.3
  vector_math: ^2.1.4
  camera: ^0.11.1
  flutter_blurhash:
    git:
      url: https://github.com/tbuczkowski/flutter_blurhash.git
    # path: ../flutter_blurhash

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  shaders:
    - shaders/gradient.frag
    - shaders/warp.frag
    - shaders/sobel.frag
    - shaders/blurhash.frag
    - shaders/math.frag
    - shaders/grayscale.frag